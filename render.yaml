services:
  - type: web
    name: aecai-api
    runtime: docker
    rootDir: backend
    envVars:
      - key: CORS_ORIGINS
        value: https://aecai.vercel.app,https://aecai-*.vercel.app,http://localhost:3000
    healthCheckPath: /api/health
    plan: free
